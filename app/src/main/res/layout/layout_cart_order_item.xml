<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="data"
            type="com.oldee.user.network.response.BasketListItem" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:orientation="vertical"
        android:paddingTop="24dp"
        android:paddingBottom="24dp">

        <LinearLayout
            android:id="@+id/ll_check"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingStart="20dp"
            android:paddingEnd="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/iv_product"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <CheckBox
                android:id="@+id/cb_sub"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:button="@android:color/transparent"
                android:background="@drawable/selector_check_blue"
                android:clickable="false" />
        </LinearLayout>

        <ImageView
            android:id="@+id/iv_product"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_marginStart="12dp"
            android:background="@drawable/bg_round_8dp_gray_2"
            app:layout_constraintBottom_toBottomOf="@id/ll_check"
            app:layout_constraintStart_toEndOf="@id/ll_check"
            app:layout_constraintTop_toTopOf="@id/ll_check" />


        <LinearLayout
            android:id="@+id/ll_info"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:orientation="vertical"
            app:layout_constraintBottom_toBottomOf="@id/iv_product"
            app:layout_constraintEnd_toStartOf="@id/iv_delete"
            app:layout_constraintStart_toEndOf="@id/iv_product"
            app:layout_constraintTop_toTopOf="@id/iv_product">

            <TextView
                android:id="@+id/tv_expert"
                style="@style/NormalText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="변화주기"
                android:textColor="@color/gray_7"
                android:textSize="12dp"
                tools:text="오래쓰기" />

            <LinearLayout
                android:id="@+id/ll_sub"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/tv_sub_title"
                    style="@style/BoldText"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="@{data.reformName}"
                    android:textSize="16dp"
                    tools:text="바지수선" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/ll_prepare_item"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/tv_prepare_item_name"
                    style="@style/NormalText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="8dp"
                    android:text="@{data.itemName}"
                    android:textSize="16dp"
                    android:visibility="gone"
                    tools:text="신발" />

                <TextView
                    android:id="@+id/tv_price"
                    style="@style/BoldText"
                    wonText="@{Integer.valueOf(data.reformPrice)}"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@color/navy"
                    tools:text="20,000원" />
            </LinearLayout>
        </LinearLayout>

        <ImageView
            android:id="@+id/iv_delete"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="24dp"
            android:src="@drawable/ic_delete"
            android:tint="@color/gray_7"
            app:layout_constraintBottom_toBottomOf="@id/ll_info"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/ll_info"
            app:layout_constraintTop_toTopOf="@id/ll_info" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
